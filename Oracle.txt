Objetivo
Dejar tu sistema en línea en una VM gratuita de Oracle, con backend, frontend y base de datos en contenedores.

1. Crear VM gratis en Oracle

Regístrate en Oracle Cloud Free Tier.
Crea una instancia Compute:
Imagen: Ubuntu 22.04.
Shape recomendado: Ampere A1 (gratis, buen rendimiento).
CPU/RAM ejemplo: 2 OCPU y 12 GB RAM (si te deja).
Activa IP pública.
Descarga la llave SSH.
2. Abrir puertos en Oracle
En la VCN de la instancia, agrega reglas de entrada:

TCP 22 (SSH)
TCP 80 (HTTP)
TCP 443 (HTTPS)
3. Entrar por SSH
Desde tu PC:

ssh -i RUTA_DE_TU_LLAVE opc@IP_PUBLICA_DE_TU_VM

4. Instalar Docker y Compose en la VM
Ejecuta:

sudo apt update && sudo apt upgrade -y
sudo apt install -y docker.io docker-compose-plugin git
sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker $USER

Luego cierra sesión y vuelve a entrar por SSH para aplicar grupo docker.

5. Subir tu proyecto
Opción A (recomendada): subirlo a GitHub y clonar en la VM:

git clone URL_DE_TU_REPO
cd NOMBRE_DEL_REPO

Opción B: copiar archivos desde tu PC con scp/rsync.

6. Configurar variables de entorno
Crea/edita el archivo de entorno de producción con:

SECRET_KEY fuerte
DEBUG=False
ALLOWED_HOSTS con tu dominio o IP
DATABASE_URL (si tu compose la usa)
Variables VITE_ para frontend si aplica
7. Levantar contenedores
Dentro del proyecto:

docker compose -f docker-compose.prod.yml up -d --build

Verifica:

docker compose -f docker-compose.prod.yml ps
docker compose -f docker-compose.prod.yml logs -f

8. Probar acceso

Abre en navegador: http://IP_PUBLICA
Si carga frontend y API responde, ya está publicado.
9. HTTPS gratis (opcional pero recomendado)
Para SSL necesitas dominio. Gratis puedes usar:

DuckDNS (subdominio gratis)
Cloudflare (DNS gratis)
Luego instalas certbot y emites certificado para tu dominio. Si quieres, te doy los comandos exactos según cómo tengas Nginx en tu contenedor.

Si quieres, en el próximo mensaje te doy una versión exacta para tu repo con:

variables listas para copiar,
comando único de despliegue,
checklist de verificación final para la entrega universitaria.


.env.prod

DEBUG=False

# Genera esto en Oracle:
# python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CAMBIA_ESTA_SECRET_KEY

POSTGRES_DB=inventario
POSTGRES_USER=inventario_user
POSTGRES_PASSWORD=CAMBIA_ESTA_PASSWORD_DB
DATABASE_URL=postgres://inventario_user:CAMBIA_ESTA_PASSWORD_DB@db:5432/inventario
DATABASE_SSL_REQUIRE=False

ALLOWED_HOSTS=localhost,127.0.0.1,163.192.120.229
CORS_ALLOWED_ORIGINS=http://163.192.120.229

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.tu-dominio.com
EMAIL_PORT=587
EMAIL_HOST_USER=usuario
EMAIL_HOST_PASSWORD=clave
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=no-reply@tu-dominio.com

# Produccion real
SEED_DEV_DATA_100=false
SEED_DEV_COUNT=100
SEED_DEV_RESET=false

# Admin inicial
DEFAULT_ADMIN_NAME=Administrador
DEFAULT_ADMIN_EMAIL=admin@armada.mil.do
DEFAULT_ADMIN_PASSWORD=CAMBIA_ESTA_PASSWORD_ADMIN

GUNICORN_WORKERS=4
FRONTEND_PORT=80
VITE_API_URL=/api/v1


/////


 ssh -i /c/Projecto/SistemadeInventario-ARD/ssh-key-2026-07-25.key ubuntu@163.192.120.229
cd SistemadeInventario-ARD/

git fetch origin
git reset --hard origin/nombre-del-branch
git reset --hard origin/develop_ger_version_v2

docker compose --env-file .env.prod -f docker-compose.prod.yml down
docker compose --env-file .env.prod -f docker-compose.prod.yml ps
docker compose --env-file .env.prod -f docker-compose.prod.yml up
docker compose --env-file .env.prod  -f docker-compose.prod.yml up -d --build
docker compose up -d --build --wait
py manage.py seed_realistic_data --force
docker exec inventario_backend python manage.py seed_realistic_data --force
docker exec inventario_backend_prod python manage.py seed_realistic_data --force
admin@armada.mil.do
Admin12345!


cat > .env.prod << 'EOF'
DEBUG=true

# Genera esto en Oracle:
# python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CAMBIA_ESTA_SECRET_KEY

POSTGRES_DB=inventario
POSTGRES_USER=inventario_user
POSTGRES_PASSWORD=inventario_pass
DATABASE_URL=postgres://inventario_user:inventario_pass@db:5432/inventario
DATABASE_SSL_REQUIRE=False

ALLOWED_HOSTS=localhost,127.0.0.1,163.192.120.229,mono59.net,www.mono59.net
CORS_ALLOWED_ORIGINS=http://mono59.net,https://mono59.net,http://www.mono59.net,https://www.mono59.net,http://163.192.120.229

# Produccion real
SEED_DEV_DATA_100=false
SEED_DEV_COUNT=100
SEED_DEV_RESET=false

# Admin inicial
DEFAULT_ADMIN_NAME=Administrador
DEFAULT_ADMIN_EMAIL=admin@armada.mil.do
DEFAULT_ADMIN_PASSWORD=Admin123456!

GUNICORN_WORKERS=4
FRONTEND_PORT=8080
VITE_API_URL=/api/v1

EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=monografico59@gmail.com
EMAIL_HOST_PASSWORD=qikgmrsjiclwvbdq
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=monografico59@gmail.com
EOF


docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build --pull always --wait

git pull
docker compose run --rm backend python manage.py migrate
docker compose up -d --build


docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build --wait && \
docker compose --env-file .env.prod -f docker-compose.prod.yml exec -T backend python manage.py migrate && \
docker compose --env-file .env.prod -f docker-compose.prod.yml exec -T backend python manage.py seed_realistic_data --force

docker exec inventario_backend python manage.py seed_realistic_data --force 
docker exec inventario_backend python manage.py seed_dev_data --force --count 500
docker exec inventario_backend_prod python manage.py seed_realistic_data --force --count 500


Si la montas como la tienes ahora (1 VM ARM + Docker + Postgres en la misma VM), puede salirte en $0/mes usando Oracle Always Free.

Resumen rápido:

$0/mes: si te mantienes dentro del Free Tier (Ampere A1, almacenamiento gratis, tráfico dentro del límite).
$5–$20/mes aprox: si te pasas un poco en disco/backups/tráfico.
$30–$120+/mes: si usas recursos pagados (VM más grande, balanceador, DB administrada, etc.).
Para tu app actual, bien optimizada y sin extras enterprise, lo normal es que quede en $0 o muy cerca de $0.
Si quieres, te hago una estimación exacta según los recursos que estás usando ahora (CPU, RAM, disco y tráfico).